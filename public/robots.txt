User-agent: *
Allow: /
Disallow: /g/
Disallow: /gw/
Disallow: /_data/
# Search results are dynamic, query-driven and produce thin/duplicate content;
# the /search page is also marked noindex. Keep it out of the crawl budget.
Disallow: /search
Disallow: /*?q=

# Every work (movies, series, anime — old + new) is now a single STATIC detail
# page under /f /d /n, so Astro's static sitemap enumerates the entire catalogue
# (~14.5k pages). There are no more SSR episode/new-work sub-sitemaps to declare.
Sitemap: https://cima-liveapp.site/sitemap-index.xml
